设计一套基于WebSocket协议的应用程序接口(API)，用于群聊机器人等服务端应用与客户端之间的实时双向通信。该API应满足以下详细功能需求和技术规范：

1. 黑名单管理功能：
- 服务端应能主动向客户端推送以下黑名单相关信息：
    a) 完整的黑名单列表（初始连接时或客户端请求时）
    b) 新增的黑名单条目（实时推送）
    c) 移除的黑名单条目（实时推送）
- 黑名单数据应包含唯一标识符及相关元数据

2. QQ号验证功能（支持网页后台配置）：
- 验证功能开关：可在网页后台启用/禁用 QQ 号验证机制
- 验证内容生成规则：
    a) 当启用验证且未配置自定义列表时，系统自动生成12位包含大小写英文字母的随机字符串
    b) 支持通过后台配置自定义验证字符串列表，字符串可包含中文、英文字符，单个字符串长度不超过99个字符
- 验证流程：
    a) 服务端在试题生成前，通过WS API向客户端发送包含用户 QQ 号和验证内容的验证请求，根据客户端返回信息确定是否弹出需要验证的提示（无需验证时不弹出）
    b) 客户端处理策略：
        i) 验证成功：客户端返回验证成功消息，服务端生成试题并开始答题流程
        ii) 验证失败：客户端返回验证失败消息，服务端提示"未能通过 QQ 号验证"，不生成试题
        iii) 无需验证：客户端返回无需验证消息，服务端不弹出验证界面并直接生成题目开始答题
        iv) 超时处理：客户端在2分钟内未给出回复，服务端根据配置执行相应操作（提示验证超时且不生成试题，或配置为允许答题）
        v) 无法验证：客户端发送无法进行验证的消息，服务端根据后台配置执行相应操作（跳过验证生成试题开始考试，或视为验证失败并给予提示）
    c) 客户端应实现自动超时处理机制，在用户未能手动发送验证消息时自动发送验证失败消息至服务端
- 验证有效期：支持在后台配置验证的有效天数，超过有效期后需要重新验证
- 白名单机制：支持配置 QQ 号白名单，白名单内的 QQ 号可跳过验证流程（网页界面列表实现参考黑名单页面）

3. 消息通知功能（每项通知支持独立启用/禁用配置）：
- 通知类型及内容规范：
    a) 同一 QQ 号短时间多次提交试题：
        - 配置项：监控时间窗口（分钟）和提交次数阈值
        - 网页显示格式："同一 QQ 号_______分钟内提交_______次试题-关/开"
        - 通知内容：包含 QQ 号、时间窗口、提交次数及具体提交时间戳
    b) 用户多次尝试登录后台失败：
        - 配置项：失败次数阈值
        - 网页显示格式："用户_______次登录后台失败-关/开"
        - 通知内容：包含每次失败的账号、密码、尝试时间戳
    c) 用户登录后台成功：
        - 通知内容：用户名、用户 QQ 号、登录时间、所属权限组
    d) 用户生成试题后短时间提交：
        - 配置项：时间阈值（分钟）
        - 网页显示格式："用户生成试题后_______分钟内提交-关/开"
        - 通知内容：用户 QQ 号、试题生成时间、提交时间、时间间隔
    e) 用户考试通过：
        - 通知内容：开始时间、结束时间、试题ID、用户 QQ 号
    f) 用户考试失败：
        - 通知内容：开始时间、结束时间、试题ID、用户 QQ 号
    g) 用户开始考试：
        - 通知内容：开始时间、试题ID、用户 QQ 号

所有WebSocket消息应采用JSON格式进行封装，包含消息类型、时间戳、数据体等必要字段，确保通信的可靠性和可解析性。